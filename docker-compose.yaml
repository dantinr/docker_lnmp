mysql: 
  image: fah-mysql

php-fpm: 
  image: fah-fpm
  volumes: 
    - /wwwroot:/wwwroot

web: 
  image: fah-nginx
  ports: 
    - "80:80"
  volumes: 
    - /wwwroot:/wwwroot
    - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
  links: 
    - php
